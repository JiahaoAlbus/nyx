# L3 DEX (v0 Skeleton)

Purpose
- Provide the DEX v0 package skeleton and module boundaries.

Scope
- Actions, state containers, receipts, replay hook, invariant hook.

Non-Scope
- Full DEX implementation (added in later weeks).
