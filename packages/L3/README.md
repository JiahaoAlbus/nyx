# L3 - Apps

Responsibilities
- Application composition (API/CLI/worker) and runtime wiring.
- Owns process boundaries and execution entrypoints.

Allowed dependencies
- L0 through L2.

Prohibited dependencies
- L4 integration adapters (keep external coupling at the edge).